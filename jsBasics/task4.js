//4.1 - Calculate the square of ​​a rectangle circle
let radius = 4;
let squareCircle = Math.PI * Math.pow(radius, 2);
console.log(`squareCircle = ${squareCircle.toFixed(2)};`);

//4.2 - Calculate the square of ​​a rectangle
let length = 7;
let width = 3.4;
let squareRectangle = length * width;
console.log(`squareRectangle = ${squareRectangle.toFixed(2)};`);

//4.3 - Calculate the volume of the cylinder
radius = 8;
let height = 4;
let cylinderVolume = Math.PI * Math.pow(radius, 2) * height;
console.log(`cylinderVolume = ${cylinderVolume.toFixed(2)};`);
